#include<stdio.h>
int main(){
    int n,i;
    scanf("%d",&n);
    if(i<=1)
    printf("Not Prime");
    if(i==2);
    printf("Prime");
while(i<=n){
    if(n%i==0)
    printf("Not Prime");
    else
    printf("Prime");
    i++;
return 0;
}
}